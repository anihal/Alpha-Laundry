# =============================================================================
# ALPHA LAUNDRY FRONTEND - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env.local for local development
# NEVER commit .env files with actual values to version control!
# =============================================================================

# API Configuration
# Point this to your backend server URL
REACT_APP_API_URL=http://localhost:3001/api

# Application Metadata
REACT_APP_NAME=Alpha Laundry
REACT_APP_VERSION=1.0.0

# =============================================================================
# PRODUCTION NOTES
# =============================================================================
# For production builds:
# 1. Update REACT_APP_API_URL to your production backend URL
# 2. Ensure the backend CORS settings allow your production domain
# 3. Consider using different .env files for different environments:
#    - .env.development
#    - .env.production
#    - .env.staging
# =============================================================================
