# =============================================================================
# ALPHA LAUNDRY - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to create your environment-specific configuration:
#   - Backend: cp .env.template backend/.env
#   - Frontend: cp .env.template frontend/.env
#
# IMPORTANT: Never commit actual .env files to version control!
# =============================================================================

# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=alpha_laundry
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# JWT Authentication
JWT_SECRET=your_jwt_secret_key_min_32_characters_recommended
JWT_EXPIRES_IN=24h

# API Configuration
API_PREFIX=/api
CORS_ORIGIN=http://localhost:3000

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# API Endpoint
REACT_APP_API_URL=http://localhost:3001/api

# App Configuration
REACT_APP_NAME=Alpha Laundry
REACT_APP_VERSION=1.0.0

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Use strong, unique passwords for production
# 2. JWT_SECRET should be at least 32 characters long
# 3. Never use default values in production
# 4. Rotate secrets regularly
# 5. Use environment-specific .env files for different deployments
# =============================================================================
